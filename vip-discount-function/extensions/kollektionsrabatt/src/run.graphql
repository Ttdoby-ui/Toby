query Input($collectionIds: [ID!], $vipTags: [String!]) {
  cart {
    buyerIdentity {
      customer {
        hasTags(tags: $vipTags) {
          tag
          hasTag
        }
      }
    }
    lines {
      id
      quantity
      cost {
        amountPerQuantity {
          amount
        }
        compareAtAmountPerQuantity {
          amount
        }
      }
      merchandise {
        __typename
        ... on ProductVariant {
          product {
            inAnyCollection(ids: $collectionIds)
            hasAnyTag(tags: ["for_vip"])
          }
        }
      }
    }
  }
  discount {
    discountClasses
    metafield(namespace: "kollektionsrabatt", key: "config") {
      jsonValue
    }
  }
}
