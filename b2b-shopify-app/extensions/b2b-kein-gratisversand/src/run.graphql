query Input {
  cart {
    attribute(key: "_b2b") {
      value
    }
    deliveryGroups {
      deliveryOptions {
        handle
        title
        cost {
          amount
        }
      }
    }
  }
}
